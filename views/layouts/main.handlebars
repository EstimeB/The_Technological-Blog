<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>The Tech Blog</title>
  {{!-- <link rel="stylesheet" href="/css/jass.css"> --}}
  <link rel="stylesheet" href="/css/style.css">
</head>

<body>
  <header class="theBar">
    <nav>
      <!-- Conditionally render login or logout links -->
      {{#if logged_in}}
      <ul class="navInline">
        <li class="pain"><a href="/">Home</a></li>
        <li class="pain"><a href="/dashboard">Dashboard</a></li>
        <li class="pain"><button class="no-button logbnt" id="logout">Logout</button></li>
      </ul>
        {{else}}
      <ul class="navInline">
        <li class="pain"><a href="/">Home</a></li>
        <li class="pain"><a href="/dashboard">Dashboard</a></li>
        <li class="slash pain">
          <a href="/login">Login</a> /
          <a href="/signup">Signup</a>
        </li>
      </ul>
        {{/if}}
      <br>
      <div class="logo">
        <span role="img" aria-label="speech-ballon" class="mImg">💬</span>
        <h1 class="shadow"><a href="/" class="mtitle">The Tech Blog</a></h1>
      </div>
    </nav>
  </header>
  <br><br>
  <main>
    <!-- Render the sub layout -->
    {{{ body }}}
  </main>

  <!-- Render script for logged in users only -->
  {{#if logged_in}}
  <script src="/js/logout.js"></script>
  {{/if}}
</body>

</html>