{{! Posts }}
<div class='text-center'>
  <div class='col-md-5 blogrow'>
    <h2 class='blogtitle'>{{name}}</h2>
    <p class='pPost'>Posted by
      {{user.user_name}}
      on
      {{format_date date_created}}</p>
  </div>
  <div class='text-center signL'>
    <p>{{post_description}}</p>
  </div>
</div>

<br />

{{! Comments }}
{{!-- {{if Comments}}
<div class="text-center row signF">
  <h2 class="none"></h2>
  <p class="signL">{{comment_des}}</p>
  <br>
  <p class="signL">-{{user.user_name}}, {{format_date date_created}}</p>
  {{else}}

  {{/if}}
</div> --}}